import 'package:flutter/material.dart';

class AddEditServantScreen extends StatelessWidget {
  const AddEditServantScreen({super.key, this.args});
  final dynamic args;

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('AddEditServantScreen - Blanked')),
    );
  }
}
