I have identified 19 static analysis issues. I need to fix these before committing.

1. Fix DI issues in `lib/core/di/injection.dart`
2. Fix `PaginationCursor` undefined class issue in `lib/features/servant/data/models/servant_models.dart`
3. Fix invalid overrides and missing implementations in `lib/features/servant/data/repo/servant_data_repository.dart`
4. Clean up `cascade_invocations` warnings in tests and source code.

I will plan these fixes.
