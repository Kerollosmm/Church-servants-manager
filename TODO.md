# Hive-First Sync Engine (P0 Task)

- [ ] Task 1: Update Data Models with Sync Metadata
    - [ ] Add `SyncStatus` enum to `core/constants/enums.dart`
    - [ ] Update `AttendanceMark` with `syncStatus` and `clientUpdatedAt`
    - [ ] Update `AttendanceSession` with `syncStatus` and `clientUpdatedAt`
    - [ ] Update `TeamModel` with `syncStatus` and `clientUpdatedAt`
    - [ ] Generate Hive TypeAdapters
- [ ] Task 2: Implement Hive Local Data Sources
    - [ ] Create `AttendanceLocalDataSource`
    - [ ] Create `TeamLocalDataSource`
    - [ ] Initialize Hive in `main.dart`
- [ ] Task 3: Refactor Repositories to Hive-First
    - [ ] Refactor `AttendanceRepository` to write to Hive first
    - [ ] Refactor `TeamRepository` to write to Hive first
    - [ ] Ensure deterministic IDs for all records
- [ ] Task 4: Implement SyncService Engine
    - [ ] Create `SyncService` with `connectivity_plus` listener
    - [ ] Implement batch sync logic (max 500 records)
    - [ ] Implement Last-Write-Wins conflict resolution
- [ ] Task 5: UI Integration & Feedback
    - [ ] Add sync status indicators to roster items
    - [ ] Add global offline/syncing banner
