# Contracts

This feature does not add or change a public API.

The contract documented here is an internal repository-behavior contract so implementation and review can verify the expected attendance mark identity rules without expanding scope into a public service definition.
