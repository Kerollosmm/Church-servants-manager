{
  "name": "security-rules-test-firestore",
  "version": "1.0.0",
  "description": "Security rules unit tests for CSMS",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "emulator:start": "firebase emulators:start --only firestore"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "jest": "^29.7.0"
  },
  "engines": {
    "node": ">=18"
  }
}
